/**
 * Udodi HTML tagged template literal.
 *
 * Currently acts as a pass-through for:
 * - Excellent syntax highlighting in editors
 * - Future extensibility (sanitization, static analysis, etc.)
 */
export const html = String.raw;