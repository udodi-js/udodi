/**
 * Udodi CSS tagged template literal.
 *
 * Used primarily for:
 * - Syntax highlighting in editors
 *
 * Currently acts as a pass-through for CSS strings, but 
 * provides a clear API for styling components.
 */
export const css = String.raw;