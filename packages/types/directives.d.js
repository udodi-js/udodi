/**
 * Each property contains the list of elements that declare the
 * corresponding directive attribute. The groups are produced by
 * `extractAllDirectives()` and consumed by the runtime binding phase.
 *
 * @typedef {Object} DirectiveGroups
 * @property {HTMLElement[]} text - Elements containing the `@text` directive.
 * @property {HTMLElement[]} bind - Elements containing the `@bind` directive.
 * @property {HTMLElement[]} on - Elements containing the `@on` directive.
 * @property {HTMLElement[]} for - Elements containing the `@for` directive.
 * @property {HTMLElement[]} if - Elements containing the `@if` directive.
 * @property {HTMLElement[]} show - Elements containing the `@show` directive.
 * @property {HTMLElement[]} class - Elements containing the `@class` directive.
 * @property {HTMLElement[]} style - Elements containing the `@style` directive.
 * @property {HTMLElement[]} attr - Elements containing the `@attr` directive.
 * @property {HTMLElement[]} ref - Elements containing the `@ref` directive.
 * @property {HTMLElement[]} teleport - Elements containing the `@teleport` directive.
 * @property {HTMLElement[]} validate - Elements containing the `@validate` directive.
 */

export {};